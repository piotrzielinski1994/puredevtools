# Learnings

Project-specific conventions, gotchas, and constraints worth recording so future-you (human or agent) doesn't re-derive them. Append-only. For architectural trade-offs use [adr.md](adr.md) instead.

## Entries

<!-- Format: one bullet per learning. Date prefix optional. -->

- 2026-07-10: A visually-hidden (`sr-only`) checkbox with the visible track/thumb as *sibling* spans is not clickable - a real mouse click on the visuals never reaches the input, so `onChange` never fires. Wrap the input + spans in a `<label>` so clicks forward to the input. Testing-library `fireEvent.click(input)` hits the input directly and hides this bug; test the label click (`getByLabelText(...).closest('label')`) to catch it. Affected `Switch` (global + per-rule toggles).
- 2026-07-10: You cannot override a **main-frame document navigation** (URL typed in the address bar) with a `fetch`/`XHR` page-layer patch - the top-level HTML loads before any script runs. Overriding it on Chrome needs `chrome.debugger` (yellow "being debugged" banner) with no cross-browser equivalent. Peer tools (MockExpress, Requestly page layer) don't do it either; they override `fetch`/`XHR` the page's JS makes. Test overrides by calling `fetch(url)` from the console, not by loading the URL as a page.
- 2026-07-10: `zod.object({...})` is non-strict by default - it silently strips unknown keys, so a stored/imported rule carrying a removed matcher field (e.g. `resourceTypes`) would pass validation. Use `.strict()` on `matchers` when the point is to *reject* legacy shapes at the import boundary.
- 2026-07-12: The sidebar folder tree ports requi's DnD with `@dnd-kit/core` **core primitives only** (`useDraggable` + `useDroppable` per row + manual pointer-Y drop projection in `tree-locate.ts`), NOT `@dnd-kit/sortable`'s `SortableContext`/`useSortable`. `SortableContext` is for a flat sortable list; a nested tree with reparenting needs the low-level primitives. Don't add `@dnd-kit/sortable` for the tree.
- 2026-07-12: The engine consumes rules purely as an **ordered array** (`repository.getAll()` -> `decide.find()` first-match) and never reads a numeric ordering field. That's why folders could be added as a storage/UI tree that `flatten`s (DFS pre-order) to `Rule[]` without touching `engine/**`, `content/bridge.ts`, `match.ts`, `decide.ts`. When adding structure over rules, preserve the flat ordered `getAll()` contract and the engine stays untouched.
- 2026-07-12: Simulating `@dnd-kit` PointerSensor drags in jsdom needs three shims/moves: (1) a `PointerEvent` shim that sets `isPrimary: true` (the activator rejects `!event.isPrimary`), (2) `Element.prototype.hasPointerCapture`/`setPointerCapture`/`releasePointerCapture` stubs, (3) stub `getBoundingClientRect` on **every** droppable row BEFORE the drag (dnd-kit measures all rects at drag start; `pointerWithin` finds no target otherwise). Drive it as pointerDown(source) -> pointerMove(document, past 5px) -> pointerMove(target) -> pointerMove(document) -> pointerUp(document). See `src/ui/test-setup.ts` + `SidebarTree.test.tsx`.
- 2026-07-18: The exported rules JSON encodes a `rewriteBody` `body` as **natural nested JSON** when it parses as a JSON object/array, else verbatim as a string (ported from purerequest's `src/lib/workspace/body-codec.ts`). In-memory the body stays a plain `string` (what the editor + engine need); the transform lives only at the export/import boundary (`src/rules/body-codec.ts` + `portable.ts` `encodeNode`/`decodeNode`), so the model/schema (`body: z.string()`), storage, and engine are untouched. Export is pretty-printed (`JSON.stringify(state, null, 2)`); import pretty-prints a decoded object body back into the string. A JSON scalar (`42`, `"x"`) or malformed `{...` stays a string on purpose. This is why the exported file shows real nested JSON, not `"{\n \\\"makes\\\"...}"`.
- 2026-07-18: The page patch matches the URL **as resolved against the page origin**, not the raw string passed to `fetch`/XHR. Apps with a basePath (e.g. Next.js `fetch('/manual-listing-creation/makes?...')` via a `withBasePath` helper) issue **relative-URL** requests; before the fix a rule written with the full `https://host/...` URL never matched (0 intercepted) even though it was a normal `window.fetch`. `resolveUrl()` (`src/engine/page/resolveUrl.ts`) now does `new URL(url, location.href).toString()` in `urlOf` (patchFetch) and `open()` (patchXhr) so full-URL rules match regardless of how the page calls fetch. The delegate still receives the original (possibly relative) input. Ground-truth debug: run the same `fetch()` in the tab console - if the manual (absolute) call intercepts but the app's does not, suspect a relative URL.
- 2026-07-19: The options/popup/DevTools pages are **extension pages under MV3 CSP `script-src 'self'` - no `unsafe-eval`**. Any dependency that does `eval`/`new Function` at runtime crashes there even though it passes vitest (jsdom) and `vite build` (Node), because both those environments have no CSP. `eslint-linter-browserify` is such a dep: its `Linter` runs rule option-schemas through `ajv`, which does `new Function`, so opening the Scripts tab threw a CSP violation. It was ported straight from `purerequest`, which is a Tauri app with `csp:null` (eval allowed). Fix: the Scripts `no-undef` linter now uses `espree` + `eslint-scope` + `globals` (acorn-based, no eval). Rule of thumb: before porting a "browser" lib from a plain-web-app sibling repo, grep the built bundle for `eval(`/`new Function(` - green tests do NOT prove it runs in the loaded extension. Only a `load-unpacked` smoke test catches CSP-eval crashes.
- 2026-07-19: Keyboard shortcuts use **`@tanstack/react-hotkeys`** (mirrors the `purerequest` repo), not a hand-rolled listener. `useHotkeys([{ hotkey: 'Mod+S', callback }])` needs a `<HotkeysProvider>` ancestor (wired at the options entry `src/ui/options/main.tsx`). Defaults matter: `preventDefault: true` and `ignoreInputs: false` for Mod/Ctrl combos, so Cmd/Ctrl+S fires even with focus inside an input **and** blocks the browser's native "Save page" dialog. A form-level React `onKeyDown` does NOT catch Cmd+S when focus is outside the form - that was the original bug. In vitest jsdom the platform reports non-mac, so `'Mod'` resolves to Control: fire the chord in tests with `userEvent.keyboard('{Control>}s{/Control}')` and wrap the render in `<HotkeysProvider>`.
- 2026-07-20: Cookie sync `set-rejected` is usually NOT httpOnly (the `cookies` API reads/writes HttpOnly fine). Two real blockers when copying to an `http://localhost` target: (1) a `__Host-`/`__Secure-` **name prefix** mandates the `Secure` attribute, which the browser forbids on http - such a cookie physically cannot be set on an http target (only fix: use an `https` target or rename). (2) `SameSite=None` (polyfill `sameSite: 'no_restriction'`) **requires** `Secure`; since `syncMapping` drops `Secure` on an http target, the browser rejects the set - fixed by downgrading `None` -> `Lax` whenever we drop `Secure` (`sameSiteFor` in `src/cookies/sync.ts`). Trade-off: `Lax` blocks the cookie on genuinely cross-site XHR, acceptable for the dev localhost case. This is why the Sync-now toast MUST surface the per-cookie skip reason (name + not-found/rejected) - an opaque "skipped 1" hides which of these three it is.
